### Phase 1- JSX + Functional Component
![alt text](image.png)

- jsx= javascript+xml(simply means use javascript inside html)

### ex:
```jsx
function App(){
    return <h1>Hello Nebula!</h1>;
}
```
```jsx
- Behind the scenes:
React.createElement("h1",null,"Hello Nebula");
```


### important concept:
- 1. always return one parents
- 2. use {} this for js
- 3. attribute should be js style:
- 4. no direct loop(use map());

```jsx
return(
    <div>
    <h1>tITLE</h1>
    <p>text</p>
    </div>
)

or 

<>
<h1>title</h1>
<p>text</p>
</>

```
```jsx
const name="Nova";
return <h1>Hello {name}</h1>
```

```jsx
classname="box" //not class like html
onClick={handleClick}
```


```jsx
 const items=["A","B","C"];

 return (
    <ul>
    {
        items.map((item)=>(
            <li key={item}>{item}</li>
        ))
    }
    </ul>
 );
 ```

### 2.Functional Component:
- 

